import pandas as pd

df = pd.read_excel("clientes.xlsx")

reporte = pd.DataFrame({
    "Campo": df.columns,
    "Vacios": df.isnull().sum().values
})

reporte.to_excel(
    "reporte_vacios.xlsx",
    index=False
)

print(reporte)
